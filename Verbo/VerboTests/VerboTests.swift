import Testing
@testable import Verbo

struct VerboTests {
    @Test func appExists() async throws {
        // Placeholder test — verifies the module is importable
        #expect(Bool(true))
    }
}
